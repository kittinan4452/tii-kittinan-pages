/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/tii-kittinan-pages",
    // output: "export",
    images: {
        unoptimized: true,
      },
  };
  
  export default nextConfig;
  